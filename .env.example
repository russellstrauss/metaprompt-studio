# Optional: Gemini API key for local dev (npm run dev). Get a key at https://aistudio.google.com
# When embedded, the host injects the key at runtime via window.__RUNTIME_CONFIG__.GEMINI_API_KEY
VITE_GEMINI_API_KEY=
